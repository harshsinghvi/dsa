#include <iostream>
#include "lib.h"

using namespace std;

int main()
{

    int arr[] = {1, 2, 0, 2, 1, 1, 1, 0, 2};
    int length = sizeof(arr) / sizeof(int);
    
    int zeros = 0;
    int ones = 0;
    int twos = 0;

    int i = 0;
    int s = 0,e = (length-1);
    
    while (i<length){
        
    }
    
    printArr(arr, length);
    // cout<<zeros<<ones<<twos<<endl;
    return 0;
}